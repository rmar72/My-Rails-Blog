
<%= form_for @article do |f| %>

  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@article.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @article.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="ui form">
<div class="ui input" >
  <p style="font-weight:bold; margin:10px 0;width:300px;">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
</div>


<div class="field" style="width:300px;">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
</div>

  <input class="ui primary button huge" type="submit" name="commit" value="Update Article" data-disable-with="Update Article" style="margin:5px 0 10px 50px;">

  </div>

<% end %>
